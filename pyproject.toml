[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "pytex"
version = "1.1"
authors = [
  { name = "Pollux" },
]
description = ""
readme = "README.md"
requires-python = ">=3.5"

[project.scripts]
pytex = "pytex:main"
